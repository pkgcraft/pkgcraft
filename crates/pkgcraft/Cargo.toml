[package]
name = "pkgcraft"
version = "0.0.10"
description = "library of Gentoo functionality"
homepage = "https://github.com/pkgcraft/pkgcraft/tree/main/crates/pkgcraft"
repository = "https://github.com/pkgcraft/pkgcraft/tree/main/crates/pkgcraft"
readme = "README.md"
license = "MIT"
autotests = false
authors.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
name = "pkgcraft"
path = "src/lib.rs"

[features]
default = []
# enable git repo syncing support
git = ["dep:git2"]
# enable repo syncing over https, e.g. tar+https
https = ["dep:flate2", "dep:reqwest", "dep:tar", "dep:tokio"]

[dependencies]
async-trait = "0.1.73"
blake2 = "0.10.6"
blake3 = { version = "1.4.1", features = ["traits-preview"] }
cached = "0.44.0"
camino = { version = "1.1.6", features = ["serde1"] }
chic = "1.2.2"
clap = { version = "4.4.0", features = ["derive"] }
crossbeam-channel = "0.5.8"
digest = "0.10.7"
enum-as-inner = "0.6.0"
filetime = "0.2.22"
flate2 = { version = "1.0.27", optional = true }
futures = "0.3.28"
git2 = { version = "0.17.2", optional = true }
glob = "0.3.1"
hex = "0.4.3"
indexmap = { version = "2.0.0", features = ["rayon", "serde"] }
indicatif = "0.17.6"
indoc = "2.0.3"
is_executable = "1.0.1"
itertools = "0.11.0"
md-5 = "0.10.5"
nix = { version = "0.27.0", features = ["user"] }
num_cpus = { version = "1.16.0", optional = true }
once_cell = "1.18.0"
peg = "0.8.1"
rayon = "1.7.0"
regex = "1.9.4"
reqwest = { version = "0.11.20", default-features = false, features = ["rustls-tls", "stream"], optional = true }
roxmltree = "0.18.0"
rust-ini = "0.19.0"
scallop = { path = "../scallop", version = "0.0.9" }
serde = { version = "1.0.188", features = ["derive"] }
serde_with = { version = "3.3.0", default-features = false, features = ["macros"] }
sha2 = "0.10.7"
strum = { version = "0.25.0", features = ["derive"] }
sys-info = "0.9.1"
tar = { version = "0.4.40", optional = true }
tempfile = "3.8.0"
thiserror = "1.0.47"
tokio = { version = "1.32.0", features = ["full"], optional = true }
toml = "0.7.6"
tracing = "0.1.37"
walkdir = "2.3.3"

# exported test support deps
assert_cmd = "2.0.12"

[dev-dependencies]
criterion = "0.5"
ctor = "0.2.4"
tracing-test = "0.2.4"

[[bench]]
name = "bench"
harness = false
